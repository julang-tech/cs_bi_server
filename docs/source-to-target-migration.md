# 客服原表 → 新表 迁移与同步设计

## 背景

客服当前仍在**原表**上做日常登记。原表把不同来源/类型的客诉跟进诉求拆在多个分表里——退款、6 美元补发、物流推进等，**字段散乱、关联信息很难一次性取出**，所以我们做了一张**新表（目标表）**，把多源、多类型的客诉统一到一个数据模型，供后续 BI / 自动化 / 跨部门跟进。

后续约定：
- **原表** = 客服当前手工录入的多张飞书表（多个来源/类型，字段不统一）
- **新表 = 目标表** = 我们统一的单表数据模型（`tbl6hthvkKaNzrvf`）
- **source-to-target 同步** = 把原表数据按映射规则转换并写入新表

## 当前同步链路

```
飞书原表（CS 录入，多分表）
    │
    │  npm run sync:source-to-target
    │  · server/domain/sync/service.ts:syncSourceToTarget
    │  · 仅读取 config.source.table_id 一张表
    │  · transformSourceRecord(rawFields) → 目标表行
    │  · enrichResultsWithShopify 补 SKU / 物流字段
    ▼
飞书新表（目标表）
    │
    │  npm run sync:run
    │  · 全量拉新表 → 写本地 SQLite (feishu_target_records)
    │  · 同时刷 Shopify BI cache（订单/退款）
    ▼
SQLite 镜像
    │
    │  P3 后端读 SQLite，joined Shopify 订单后输出
    ▼
P3 客诉总览看板
```

**配置**（`config/sync/config.json`）：

| 角色 | app_token | table_id | view_id |
|---|---|---|---|
| 源表 | `Gv0LwToRZiPhynkpMUHcMsVWn2c` | `tblQsSMgz7jtC7mI` | `vewEEJpUSV` |
| 目标表 | `KfJGb8qDUaNVPCsC01oc4iAlnEc` | `tbl6hthvkKaNzrvf` | `vewdebCvBd` |

⚠️ 当前 `config.source` **只指了一个 table_id**——意味着 source-to-target 只同步原表里的一张分表（即"退款登记"那张）。其他分表（补发、物流推进等）还没接入。

## 已识别的 4 个问题（2026-05）

### 问题 1：`VIEW_HIT_MAP` 漏映射，约 30% 退款分类无视图

`server/domain/sync/transform.ts:32` 的 `VIEW_HIT_MAP` 只覆盖 5 种「退款原因分类」：

```
产品问题   → 1-3待跟进表-货品瑕疵
缺货问题   → 1-2待跟进表-漏发、发错
物流问题   → 1-4待跟进表-物流问题
错漏发     → 1-2待跟进表-漏发、发错
瑕疵问题   → 1-3待跟进表-货品瑕疵
```

`COMPLAINT_TYPE_MAP` 知道但 `VIEW_HIT_MAP` 不知道的：`券/折扣问题`、`订单异常/取消/修改订单`、`高风险订单`、`其他`——这类客诉到目标表后**「命中视图」字段为空**。

### 问题 2：`1-5待跟进表-补发` 后端不识别

source-to-target 在「具体操作要求」包含"补发"时会写视图 `1-5待跟进表-补发`，但 P3 后端 `ISSUE_VIEW_TO_MAJOR_TYPE`（`server/integrations/sqlite.ts:79`）只认 `1-2 / 1-3 / 1-4`，**所有补发记录被 P3 静默丢弃**。

### 问题 3：双依赖且无 fallback

`inferViewHit` 强依赖「具体操作要求」**和**「退款原因分类」两个字段；任一空 + 走不到查表，命中视图必为空。

### 问题 4：丢弃静默化

被丢的记录只在 `notes` 数组里写一行 `Skipped sqlite mirrored record X: unable to map`。看板 UI 完全看不到，排查全靠扒日志/SQLite。

---

## 工作计划

### 1. 数据同步与规则制定

#### 1.a 列出原表需要同步的分表（待用户确认）

当前同步只覆盖 1 张原表分表（`tblQsSMgz7jtC7mI`，即"退款登记"）。需要确认：

- [ ] 原表的飞书 app token 还包含哪些分表？（例如：6 美元补发表、物流推进表、客户回访表 …）
- [ ] 哪些**确实需要**同步进新表？哪些保持独立？

收集方式：
- 用户列举（最快）
- 或者用飞书 OpenAPI 的 [list bitable tables](https://open.feishu.cn/document/server-docs/docs/bitable-v1/app-table/list) 拉清单（需要扩 sync config 增加多 source table 支持）

#### 1.b 每张原表分表的映射规则

每张原表分表都要有一份映射规则文档，至少包括：
- 原表字段 → 目标表字段（含必填校验、值映射如 `产品问题 → 1-3待跟进表-货品瑕疵`）
- 分表对应的目标表「命中视图」（如果是新视图，先按 §2 补建）
- 是否需要按 SKU 拆行（一条原表可能拆出 N 条目标表）
- 是否需要 Shopify enrichment（订单/物流补齐）

代码层做法：将 `transformSourceRecord` 改为**按源 table_id 分派的策略表**，每张表一份独立 transformer。

### 2. 视图完善

#### 2.a 新增「待跟进表-退款」视图（确认要做）

新表当前缺一个**纯退款**的跟进视图，需要在飞书新表里建：

- 视图名：`1-1待跟进表-退款`（或团队习惯的命名）
- 筛选条件：命中视图 = "退款"（或映射规则里指向这个视图的所有客诉类型）
- 后端 `ISSUE_VIEW_TO_MAJOR_TYPE` 需要新增映射（建议归到 `refund` 或 `product` 之一，待定义）

#### 2.b 评估其他可能缺的视图（待用户输入原表能解决的诉求清单）

需要等原表分表清单（§1.a）出来后，才能逐张比对原表"能解决的诉求 vs 新表已有视图"，找出 gap。

候选可能要补的视图：
- 待跟进表-退款（已知）
- 待跟进表-赔付/代金券（如果原表有这类）
- 待跟进表-账户安全/高风险订单
- 已结案归档视图（区分 active vs archived）

---

## Open Questions

1. **原表的所有分表**有哪些？✅ 已用飞书 API 拉清单（11 张），见下文「原表分表清单与同步范围」
2. **新建视图的命名规范**——继续用 `N-X待跟进表-<类型>` 还是新规范？
3. **退款记录最终归 BI 哪类**——product / warehouse / logistics / 新增 `refund` 第四类？
4. 原表录入完成后，**是否仍保留原表作为录入入口**，还是逐步把录入也迁到新表？

---

## 原表分表清单与同步范围（2026-05-02 拍）

| 飞书表名 | 记录 | 同步？ | 落到新表的视图 |
|---|---:|---|---|
| 退款登记 | 4,222 | ✅ 同步（已） | 待跟进表-退款 / 货品瑕疵 / 漏发发错 / 物流问题（按"退款原因分类"分流）+ 补发（按"具体操作要求"含"补发"分流） |
| 6美元补发订单 | 1,271 | ✅ 同步（新增） | 待跟进表-补发 |
| 物流问题 | 443 | ✅ 同步（新增） | 待跟进表-物流问题 |
| 手工退货登记 | 227 | ✅ 同步（新增） | 待跟进表-退款 |
| 瑕疵反馈 | 68 | ✅ 同步（新增） | 待跟进表-货品瑕疵 |
| 错发反馈 | 66 | ✅ 同步（新增） | 待跟进表-漏发发错 |
| ~~拒付订单信息~~ | 25 | ❌ 不同步 | 已不再使用；后续若需要归"支付争议"视图 |
| ~~被税~~ | 1 | ❌ | 1 条疑似测试 |
| ~~线下通知缺货SKU~~ | 8 | ❌ | SKU 维度而非订单维度，跟客诉模型不匹配 |
| ~~OT32错版问题~~ | 36 | ❌ | 一次性运营登记 |
| ~~待办需每日跟进~~ | 145 | ❌ | 工作流跟进表非数据源 |

**新视图**：仅新建 `1-1待跟进表-退款`（覆盖纯退款 + 手工退货 + 退款登记里"其他/折扣/订单异常"等无明确归类的退款），其他视图复用现有。**已建 ✓**

> 视图本身只是同一张大表的展示筛选，**不影响字段映射**。下面映射规则里的「命中视图 = X」是写入那个 multi-select 字段的值，是否在视图里展示由飞书侧筛选决定。

---

## 跟进组自动判定规则（适用全部 6 张表，覆盖原"客服组"硬编码）

| 命中视图 | 跟进组 |
|---|---|
| 1-2待跟进表-漏发、发错（缺货/错发） | 仓库组 |
| 1-3待跟进表-货品瑕疵 | 采购组 + OEM组 + 商品组 + 财务组（4 个组） |
| 1-4待跟进表-物流问题 | 物流组 + 财务组 |
| 1-5待跟进表-补发 | 仓库组 |
| 1-1待跟进表-退款（纯退款） | 财务组 |

---

## 各源表 → 目标表 映射规则（最终版）

### 共享行为

- **创建人字段** = 客服跟进人（从源表"创建人"/"反馈人"取，缺失时与"反馈/录入人"一致）
- **推送状态** = `pending`
- **问题处理状态** = `待处理`
- **跟进组** = 按"命中视图"自动判定（见上表）
- **父记录字段** 一律忽略
- **每张表的"客诉类型"如果源端没值** → 按 SKU 推断（结合 SKU 形态）或 fallback 到"商品尺码"
- **客诉方案**：默认按文本推断；推断不到 fallback 到"全额退款"（瑕疵/错发场景）或"退货退款"（手工退货场景）

### 1️⃣ 退款登记 (`tblQsSMgz7jtC7mI`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 订单号 | 订单号 | 直传 |
| 记录日期 | 记录日期 | 直传 |
| 客户订单总数 | 历史订单数 | 直传 |
| 物流状态 | 物流状态 | LOGISTICS_STATUS_MAP |
| 是否退运费 / 是否退运费险 | 同名 | REFUND_FLAG_MAP |
| 退款原因分类 | 客诉类型 + 命中视图 | 见下方"COMPLAINT_TYPE_MAP & VIEW_HIT_MAP 最终版"；缺失映射 fallback 到"客户原因-尺码不合适" + 命中视图"1-1待跟进表-退款" |
| 具体操作要求 | 具体金额/操作要求 + 客诉方案（推断）+ 解析 SKU 拆行 → 客诉SKU | 现有 |
| 备注 | 待跟进客诉备注 | **直传**（取消"拼上退款原因分类"） |
| 最终退款金额（美元） | **SKU退款金额** | 见下方"SKU 维度退款金额计算" |
| 是否收到退货/退货单据 | （拆字段）**退货单号**（需新建/复用现有"退货单号"字段） | **拆**：截图归"退货/瑕疵凭证原图"，单号归"退货单号" |
| 创建人 | 客服跟进人 | User 字段直传 |
| 父记录 | — | 丢 |

**SKU 维度退款金额计算**：
1. 解析"具体操作要求"得到 N 个 SKU
2. 每个 SKU 行：`SKU退款金额` = `SELECT discounted_total_usd FROM shopify_bi_order_lines WHERE order_no=X AND sku=Y`
3. 即"该 SKU 的实际支付金额（已扣折扣）"，全额退款

**飞书表头改名**：`退货凭证原图` → `退货/瑕疵凭证原图`（影响表 4、5）✓ 已改

---

### COMPLAINT_TYPE_MAP & VIEW_HIT_MAP 最终版（基于实际枚举）

`server/domain/sync/transform.ts` 现有的两张表要更新：

```typescript
// 退款原因分类 → 客诉类型
const COMPLAINT_TYPE_MAP: Record<string, string> = {
  产品问题: '货品瑕疵-其他',
  '券/折扣问题': '客户原因-折扣码退款',
  缺货问题: '仓库-缺货',
  '订单异常/取消/修改订单': '客户原因-重复下单/下错单取消',
  物流问题: '物流问题-其他',
  错漏发: '仓库-漏发',
  瑕疵问题: '货品瑕疵-其他',
  高风险订单: '高风险订单',
  其他: '客户原因-其他',
}
const COMPLAINT_TYPE_FALLBACK = '客户原因-尺码不合适'

// 退款原因分类 → 命中视图
const VIEW_HIT_MAP: Record<string, string> = {
  产品问题: '1-3待跟进表-货品瑕疵',
  瑕疵问题: '1-3待跟进表-货品瑕疵',
  缺货问题: '1-2待跟进表-漏发、发错',
  错漏发: '1-2待跟进表-漏发、发错',
  物流问题: '1-4待跟进表-物流问题',
  // 全部退款类（券/折扣 / 订单异常 / 高风险 / 其他 / 任何未识别）→ 1-1
}
const VIEW_HIT_FALLBACK = '1-1待跟进表-退款'
```

> **`操作要求里有"补发"** 仍然 override 命中视图为 `1-5待跟进表-补发`。

### 2️⃣ 6美元补发订单 (`tblnLyB0ouC1d5SL`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 原订单号 | 订单号 | |
| 日期 | 记录日期 | |
| 客户姓名 | 客户姓名 | |
| 需补发SKU | 客诉SKU | |
| 补发订单号 | 补发订单号 | |
| 客诉原因（多选 e.g. ["poor fit","Size too large"]） | 待跟进客诉备注（拼） + **推断客诉类型** | **不**用"补发-6美元"；按文本推断到现有客诉类型（尺码不合适、款式不喜欢…），fallback "商品尺码" |
| 创建人 | 客服跟进人 | |

**固定值**：
- 命中视图 = `1-5待跟进表-补发`
- 客诉方案 = `["6美元补发"]`（用专属枚举值）
- 跟进组 = 仓库组
- 推送状态 = `pending`
- 问题处理状态 = `待处理`

**客诉类型推断规则**（按"客诉原因"文本，命中即停）：

| 关键词 | → 客诉类型 |
|---|---|
| size / fit / 尺码 / too large / too small / loose / tight | 客户原因-尺码不合适 |
| style / 款式 / color / 颜色 / 颜色不喜欢 / don't like / dislike | 客户原因-款式不喜欢（颜色合并到这里） |
| **fallback** | **客户原因-尺码不合适** |

### 3️⃣ 手工退货登记 (`tblf4JJZOyq4q0nl`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 订单号 | 订单号 | |
| 记录日期 | 记录日期 | |
| 客诉SKU | 客诉SKU | |
| 客诉原因（多选） | 待跟进客诉备注（拼）+ **推断客诉类型**（尺码/颜色/款式…），fallback "商品尺码" | |
| 顾客三围信息 | 待跟进客诉备注（追加）；基本为空可忽略 | |

**固定值**：
- 命中视图 = `1-1待跟进表-退款`
- 客诉方案：fallback 到 `["全额退款"]`
- 跟进组 = 财务组
- 状态字段同上

**客诉类型推断规则**：同 §2 的关键词表（尺码/款式/颜色/fallback 尺码不合适）。

**"待跟进表-退款"视图的扩展逻辑**：除了来自手工退货的，**所有命中视图为 1-1 的退款类记录都进这个视图**（即原"退款登记"里被丢掉那批"券/折扣/订单异常/其他"）。视图本身的筛选条件由飞书侧管。

### 4️⃣ 瑕疵反馈 (`tblchKVT2yi7EmeB`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 订单号 | 订单号 | |
| 反馈日期 | 记录日期 | |
| 产品sku | 客诉SKU | |
| 瑕疵说明 | **待跟进客诉备注**（不写入"瑕疵原因"——预留给内部填） | 同时**根据瑕疵说明文本推断客诉类型** |
| 订单发货时间 | 订单发货时间 | |
| 反馈人 | 客服跟进人（创建人）；缺失时 = 反馈人本人 | |
| （照片）若源表带瑕疵图 | 退货/瑕疵凭证原图 | 字段复用 |

**固定值**：
- 命中视图 = `1-3待跟进表-货品瑕疵`
- 客诉方案：默认 `["补发"]`；推断不到 fallback `["全额退款"]`（按"我们的职责，没填就退全款"原则）
- 跟进组 = 采购组 + OEM组 + 商品组 + 财务组

### 5️⃣ 错发反馈 (`tbloBJUqkCQgllhm`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 订单号 | 订单号 | |
| 反馈日期 | 记录日期 | |
| 产品sku | 客诉SKU | |
| 错发说明 | **待跟进客诉备注**（不写入"错/漏发原因"——预留） | **根据错发说明推断客诉类型**（错发 vs 漏发） |
| 订单发货时间 | 订单发货时间 | |
| 反馈人 | 客服跟进人 | |
| （照片）若有 | 退货/瑕疵凭证原图 | |

**固定值**：
- 命中视图 = `1-2待跟进表-漏发、发错`
- 客诉方案：默认 `["补发"]`；fallback `["全额退款"]`
- 跟进组 = 仓库组

### 6️⃣ 物流问题 (`tbldrg1cYmPOj51k`)

| 源字段 | → 目标字段 | 说明 |
|---|---|---|
| 订单号 | 订单号 | |
| 日期 | 记录日期 | |
| 物流号 | 物流号 | |
| 物流问题 | 待跟进客诉备注 | （**不**写"物流-跟进过程"；那是过程） |
| 跟进1 + 跟进2 | 物流-跟进过程（合并，分隔符如 ` | `） | 源表把"跟进2"误用作结果列 |
| —（无值） | 物流-跟进结果 | 留空 |

**固定值**：
- 命中视图 = `1-4待跟进表-物流问题`
- 客诉类型：根据"物流问题"文本推断；fallback `物流问题-其他`
- 客诉方案：源端无；fallback `["退货退款"]` 或留空（待定）
- 跟进组 = 物流组 + 财务组
- 是否退运费 / 是否退运费险：物流问题不退款，留空

---

## 用 SQL 自动补全字段（回答用户最后那个问题）

源表 6 张字段都很少（5-10 个），目标表 43 个字段，**绝大多数缺的字段可以从 Shopify 数据查出来**。已有的 enrichment 链路（`enrichResultsWithShopify`）已经在做了，但只补 9 个字段，还可以扩展。

### 现状：enrichResultsWithShopify 现在补的字段（来自 Shopify Admin API 实时调用）

`server/domain/sync/service.ts:376` — 9 个字段：

```
客户姓名, 客户邮箱, 下单日期, 订单金额, 物流号, 订单发货时间,
后台订单链接, 物流状态, SKU金额
```

### 可以扩展补的字段（来自 SQLite Shopify BI cache）

cache 里有 47K 订单 + 127K 行项目，可以直接 join 查：

| 目标字段 | 数据源 | SQL |
|---|---|---|
| **SKU退款金额** | `shopify_bi_order_lines.discounted_total_usd` | `SELECT discounted_total_usd FROM shopify_bi_order_lines WHERE order_no=? AND sku=?` |
| **订单累计退款** | `shopify_bi_refund_events` SUM | `SELECT SUM(refund_subtotal_usd) FROM shopify_bi_refund_events WHERE order_no=?` |
| **下单日期 / 订单金额** | `shopify_bi_orders` | 已有 cache 路径，可不用打 Admin API |

### 补全策略

把 enrichment 改成两段：

1. **快路径（SQLite cache）** — 优先用本地缓存填能填的（SKU退款金额、订单累计退款、下单日期、订单金额、SKC、SPU 等）
2. **慢路径（Shopify Admin API）** — 只补 cache 里没有的（客户姓名、客户邮箱、物流号、物流状态、订单发货时间、后台订单链接、退货凭证图等）

收益：
- 同步速度大幅提升（少打 Admin API）
- 离线/Admin API 限流时仍能补大部分字段
- 数据一致性更好（cache 跟 BI 看板共用同一份数据）

### 仍然填不上的字段（非源表也非 Shopify）

| 字段 | 怎么办 |
|---|---|
| 处理人, 客服跟进人（除"创建人"外） | 由人工后续填 |
| 解决方案, 错/漏发原因, 瑕疵原因 | 内部跟进时填（按 §4-5 的设计预留） |
| 退货凭证原图 | 客户提供时附件上传 |
| 推送对象, 推送时间, 退货单号 | 后续工作流写入 |

---

## 行动顺序

1. ✅ 拉原表清单 + 拍同步范围（本次完成）
2. ✅ 各表映射规则（本次完成）
3. ✅ 飞书新表加视图 + 字段调整（用户已完成）
4. ⏭️ 后端 `跟进组` 改成视图驱动的 `FOLLOW_UP_TEAM_BY_VIEW`
5. ⏭️ `transformSourceRecord` 重构成按 `table_id` 分派的 strategy；写 6 个 transformer
6. ⏭️ enrichment 扩成"先 SQLite 后 Admin API"两段
7. ⏭️ `npm run sync:source-to-target` 改成接受多 source table（数组配置 + 循环跑）
8. ⏭️ 跑全量同步 + 抽样校验

---

## 修复优先级（独立于上面的工作计划，可以并行）

| | 修复 | 影响 |
|---|---|---|
| **P0** | 后端 `ISSUE_VIEW_TO_MAJOR_TYPE` 加 `1-5待跟进表-补发`（归 warehouse 或新增 refund 类） | 立刻找回所有补发记录 |
| **P0** | `VIEW_HIT_MAP` 补全 `券/折扣/订单异常/高风险/其他` → 兜底视图 | 立刻找回 ~30% 漏分类记录 |
| **P1** | 看板顶部 surface "X 条记录因字段缺失被丢弃"提示 + 详情链接 | 数据可见性 |
| **P2** | 多源 transformer 重构（按 table_id 分派） | 配合 §1 工作计划 |
